{
  "name": "smartshopper-turbo",
  "private": true,
  "workspaces": ["apps/*"],
  "devDependencies": {
    "turbo": "^1.10.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build"
  }
}
